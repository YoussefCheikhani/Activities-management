
{{ include('pages/inc/header.html.twig') }}
	<div class="container">
  <br>
	<h3>Liste des participants</h3>
		<table class="table table-hover">
  <thead>
  <div class="row">
      <a href="{{ absolute_url(asset('')) }}app_dev.php/participant/create" class="btn btn-success">Ajouter Participant</a>
    </div>
    <tr>
      <th scope="col">Num participant</th>
      <th scope="col">Nom</th>
      <th scope="col">Prenom</th>
      <th scope="col">Opération</th>
    </tr>
  </thead>
  <tbody>
    {% for participant in participants %}
    <tr class="table-active">
      <th scope="row">{{ participant.num }}</th>
      <td>{{ participant.nom }}</td>
      <td>{{ participant.prenom }}</td>
      <td>
        <a href="" class="btn btn-primary">modifier</a>
        <a href="{{ absolute_url(asset('')) }}app_dev.php/participant/delete/{{ participant.id }}" class="btn btn-danger">Supprimer</a>
      </td>
    </tr>
    {% else %}
      <tr>
        <td>Aucun participant pour l'instant</td>
      </tr>
    {% endfor %}
  </tbody>
</table> 
	</div>

{{ include('pages/inc/footer.html.twig') }}

